package main

import "fmt"

struct card {
    var number int;
    var suit int;
}

func main() {

    cards := make(map[int]card)
    // represent different suits by just adding up and running % 13 to see if they're
    // in order

}
